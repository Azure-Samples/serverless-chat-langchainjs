# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: serverless-ai-langchainjs
metadata:
  template: serverless-ai-langchainjs@1.0.0

services:
  webapp:
    project: ./packages/webapp
    dist: dist
    language: ts
    host: staticwebapp
    hooks:
      predeploy:
        posix:
          shell: sh
          # TODO build app with API url
          run: echo ""
          continueOnError: false
          interactive: false
        windows:
          shell: pwsh
          # TODO build app with API url
          run: echo ""
          continueOnError: false
          interactive: false

  api:
    project: ./packages/api
    language: ts
    host: function

hooks:
  postup:
    # TODO curl to ingest the data
    run: |
      echo "Post up hook"
    interactive: true
    continueOnError: false
